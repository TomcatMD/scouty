<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Scouty - Job Match Report</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f8fafc;
    }

    .company-card {
      margin-bottom: 2rem;
      border-radius: 1rem;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
    }

    .score-badge {
      font-size: 1.2rem;
      font-weight: bold;
      padding: 0.5em 1em;
      border-radius: 2em;
      color: #fff;
      background: linear-gradient(90deg, #38bdf8 0%, #6366f1 100%);
      margin-right: 1em;
    }

    .score-badge.high {
      background: linear-gradient(90deg, #22c55e 0%, #16a34a 100%);
    }

    .score-badge.medium {
      background: linear-gradient(90deg, #facc15 0%, #eab308 100%);
    }

    .score-badge.low {
      background: linear-gradient(90deg, #94a3b8 0%, #64748b 100%);
    }

    .job-link {
      text-decoration: none;
      color: #2563eb;
      font-weight: 500;
    }

    .job-link:hover {
      text-decoration: underline;
    }

    .notes {
      color: #64748b;
      font-size: 0.98em;
    }

    .collapse-toggle {
      cursor: pointer;
      color: #6366f1;
      font-weight: 500;
    }
  </style>
</head>

<body>
  <div class="container py-5">
    <h1 class="mb-4 text-center fw-bold">Scouty - Job Match Report</h1>

    <% if report.companies.empty? %>
      <p class="text-center text-muted mb-5">No job positions have been scraped and reviewed yet.</p>
    <% else %>
      <p class="text-center text-muted mb-5">Top job matches for your profile, grouped by company. Click to view all positions for each company.</p>
    <% end %>

    <% report.companies.each_with_index do |company, index| %>
      <div class="company-card bg-white p-4 mb-4">
        <div class="d-flex align-items-center mb-2">
          <span class="score-badge <%= company_badge_class(company) %>"><%= company.top_job_score %></span>
          <h2 class="h4 mb-0"><%= company.name %></h2>
        </div>
        <div>
          <ul class="list-group list-group-flush mb-2">
            <% top_company_jobs(company).each do |job| %>
              <li class="list-group-item px-0">
                <a class="job-link" href="<%= job.url %>" target="_blank"><%= job.position %></a>
                <span class="badge <%= job_badge_class(job) %> ms-2"><%= job.score %></span>
                <div class="notes"><%= job.notes %></div>
              </li>
            <% end %>
          </ul>
          <% if more_company_jobs?(company) %>
            <span class="collapse-toggle" data-bs-toggle="collapse" href="#company-more-jobs-<%= index %>" role="button" aria-expanded="false" aria-controls="company-more-jobs-<%= index %>">More ...</span>
            <ul class="list-group list-group-flush collapse mt-2" id="company-more-jobs-<%= index %>">
              <% more_company_jobs(company).each do |job| %>
                <li class="list-group-item px-0">
                  <a class="job-link" href="<%= job.url %>" target="_blank"><%= job.position %></a>
                  <span class="badge <%= job_badge_class(job) %> ms-2"><%= job.score %></span>
                  <div class="notes"><%= job.notes %></div>
                </li>
              <% end %>
            </ul>
          <% end %>
        </div>
      </div>
    <% end %>

    <footer class="text-center mt-5 text-muted">
      <small>Generated by Scouty &mdash; <a href="https://github.com/TomcatMD/scouty" target="_blank">GitHub</a></small>
    </footer>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
